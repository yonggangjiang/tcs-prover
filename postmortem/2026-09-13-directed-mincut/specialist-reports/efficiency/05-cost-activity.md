## Cost classification of run 2026-09-13_17-06-14 (directed min-cut), by activity and mathematical yield

Scripts and tables (mine, in the shared eff/ dir): `/private/tmp/claude-503/-Users-yonggangjiang-Downloads-tcs-prover/62d437f4-1343-4f50-b460-a8439c134571/scratchpad/eff/{parse.py,calls.py,classify.py,analyze2.py,calls_classified.json,calls_dump.txt,tables_activity.txt,tables_duplicates.txt,results_preview.txt}`. All times UTC, 2026-09-13.

### 0. Accounting facts (measured from transcript.jsonl)
- 2,770 `thread/tokenUsage/updated` events, but only 2,223 carry a changed cumulative total; the other 547 repeat the previous `last` block byte-for-byte (Codex re-emits usage per completed item inside one response, e.g. ROOT 15:07:17, 15:07:29, 15:07:45, 15:07:51, 15:07:58 all `in=42305 out=238`). Summing `last` only over changed events reproduces the brief's 269.78M exactly (cached 258.90M, uncached 9.05M, output 1.83M incl. 0.91M reasoning). My grouping "items completed since the previous changed event" gives 2,228 call groups ("calls" below); token attribution is exact, item attribution is by adjacency.
- Cumulative totals reset at 17:49:31 (ROOT), 17:51:58/17:52:04 (three subagents) and 20:13:34 (ROOT): the two post-audit resumes. Per-thread: ROOT 656 calls/79.56M/avg ctx 120.5k; literature_status 356/47.29M/132k; structural_approaches 297/35.26M/118k; flow_reduction 285/31.26M/109k; batch_supersets 265/31.21M/117k; audit_cleanup 207/26.60M/128k; approximation_proofs 162/18.59M/114k. Context percentiles: p50 118k, p75 162k, p90 193k, max 240k.
- Cost model (assumption, stated once): GPT-5-class list prices $1.25/M input, $0.125/M cached, $10/M output. Run ≈ $62 at standard tier; the run used `service_tier="fast"` (workflow_runner.py `speed_arguments`, README "OpenAI's 1.5x Fast mode") so ≈ $93. Shares under that model: cached input 52%, output 30%, uncached 18%. If the Codex subscription quota weights cached tokens at full price, cached input is 96% of the bill; I therefore report token shares as the primary metric and $ only as a secondary check.
- Context burden model: 6.32M tokens were *added* to contexts by tool output/messages; because every added token is re-sent on every later call until compaction (mean compaction segment = 60 calls), those additions account for 202.8M of the 269.8M tokens. Reading is the cost driver, not thinking.

### 1. Activity x calls x share (all 7 threads; tokens = total tokens of the calls)
| activity | calls | % calls | Mtok | % tok | reasoning items (math / verify / process) |
|---|---|---|---|---|---|
| subagent interim message to root ("report to root") | 464 | 20.8 | 56.39 | 20.9 | 1001 / 65 / 83 |
| instruction messages (root->sub 292 = 36.67M; sub->sub 133) | 425 | 19.1 | 54.35 | 20.1 | 609 / 36 / 92 |
| read node files (APPROACHES/A###) | 356 | 16.0 | 42.88 | 15.9 | 315 / 41 / 122 |
| literature: 209 searches/fetches/in-page finds | 209 | 9.4 | 26.04 | 9.7 | 170 / 28 / 47 |
| final result or status message (117 sub results, 4 root) | 121 | 5.4 | 14.88 | 5.5 | 34 / 13 / 64 |
| migration after INITIAL_PROMPT.md edits (15:32-15:34, 16:02-16:12, 16:46-16:56) | 106 | 4.8 | 14.60 | 5.4 | 47 / 10 / 57 |
| write node files | 99 | 4.4 | 13.03 | 4.8 | 137 / 11 / 27 |
| re-read INITIAL_PROMPT.md (dedicated calls; 158 read commands in total) | 97 | 4.4 | 10.42 | 3.9 | 16 / 7 / 27 |
| index/link validation + hash checks (90 ad-hoc python scripts) | 67 | 3.0 | 8.87 | 3.3 | 71 / 9 / 38 |
| read other (grep, listings, prompts) | 39 | 1.8 | 5.29 | 2.0 | |
| read index.md / PROVED.md | 45 | 2.0 | 4.69 | 1.7 | |
| thinking only (reasoning then message, no tool) | 39 | 1.8 | 4.59 | 1.7 | 37 / 3 / 15 |
| numeric checks (python on small cases) | 30 | 1.3 | 3.22 | 1.2 | 28 / 6 / 6 |
| idle/wait polls (collab `wait`) | 21 | 0.9 | 2.92 | 1.1 | |
| post-audit resume recovery (ROOT 17:49, 20:13) | 18 | 0.8 | 2.82 | 1.0 | |
| read audit reports (30 calls; 73 reads of the 55 KB report set = 1.1 MB injected) | 30 | 1.3 | 2.35 | 0.9 | |
| compaction summaries (30) | 30 | 1.3 | 0.94 | 0.3 | |
| compaction recovery reads (26) | 26 | 1.2 | 0.90 | 0.3 | |
| write PROVED.md / other | 6 | 0.3 | 0.62 | 0.2 | |
Reasoning-summary items overall: 3,577 = math 2,555 (71%), verify-math 248 (7%), process 643 (18%), unclassified 131. ROOT alone: delegation 292 calls/36.67M, node reads 116/14.01M, validation 44/5.43M, migration 33/4.64M, write 32/3.84M, literature 28/3.44M, resume recovery 18/2.82M, audits 12/0.72M.

Subagent results, all 125 classified by lead content (task window = previous result on the same thread to this result; calls attributed by thread+time):
| class | tasks | calls | Mtok | % of run |
|---|---|---|---|---|
| writing proofs into records ("Created/Completed A0xx with complete proofs") | 40 | 643 | 75.12 | 27.8 |
| verification of a sibling's or root's lemma/node | 49 | 408 | 53.05 | 19.7 |
| literature | 11 | 207 | 26.38 | 9.8 |
| new mathematics (result states a new lemma/construction/counterexample) | 18 | 168 | 17.42 | 6.5 |
| formatting/migration (#26-28, #44-47) | 7 | 84 | 11.17 | 4.1 |
| after last result (unfinished) | - | 62 | 7.06 | 2.6 |
Mapping: new-math = #2,3,6,7,8,13,14,15,18,30,56,60,76,81,98,109,113,114; literature = #1,50,61,86,91,97,101,102,105,111,125; formatting = #26,27,28,44,45,46,47; verification = 49 listed in classify.py; the rest writing. Note that "writing" tasks often contain real proving (A025, A026, A030 ...) directed by root; their cost is inflated by the self-contained-record rule, not by zero yield.

### 2. Ten most expensive low-yield episodes (what a rule would have prevented)
1. Interim messaging protocol, whole run: 464 subagent->root interim messages (56.4M, avg ctx 120k; median 3 per task, max 14) and 292 root replies (36.7M, avg ctx 125k) = 93M, 34% of the run. Each message forces a full-context call on the other side. Rule: one spawn = one brief = one final report; no interim messages, no root steering of a running subagent (E1).
2. Verification task #124 (audit_cleanup 21:46:00-21:58:59, 48 calls, 8.30M): third pass over A055 after #118 (21:34:57) and #119 (21:45:20) had passed it; found "minor bookkeeping and boundary corrections". Rule: verify once at promotion (E2).
3. Root re-reading nodes right after subagent results: 86 of 116 ROOT node-read calls (11.2M) fall within 3 min of a subagent result, 60 of them (7.7M) right after a *verification* result, i.e. a third review layer on lemmas two agents had already passed. Rule: subagent returns a <=300-word abstract + verdict; root does not reopen a node after PASS (E1/E2).
4. Migration after external INITIAL_PROMPT.md edits: 106 calls, 14.6M (5.4%) across 15:31:54-15:33:35 (7 root calls, 1.19M), 16:02:04-16:12:00 (root's whole remaining turn plus tasks #26-28) and 16:46-16:56 (tasks #44-47; formatting tasks total 84 calls, 11.17M). Rule 9 (never mass-migrate) is now in the YAML; add runner freeze (E7).
5. Literature task #50 (literature_status 17:52:03-18:01:22, 32 calls, 5.11M) re-establishing after the audits that "JLSW packing is conditional" and deterministic max-flow status: literature status about openness, not a primitive. Strategy rule 7 (open-problem statements are not evidence) prevents it.
6. One paper fetched 25 times: arXiv 2407.10830 (decremental graphs) opened by literature_status 14x, flow_reduction 10x, ROOT 1x between 17:52 and 19:48 (25 calls, 2.92M, plus in-page finds). Overall 65 fetches of 11 arXiv ids that were each opened >=3 times. Rule: fetch once into shared notes (E6).
7. Literature task #102 (batch_supersets 20:51:57-21:01:04, 30 calls, 4.30M) reading the CLNPS proof (2309.16629 fetched 5x across two threads) to conclude "neither L057 nor L061 eliminates either cost". Same rule as 5/6.
8. Index/link validation: 67 calls, 8.87M (ROOT 44, 5.43M); the model wrote 63 link-checker and 27 hash-check python scripts (only 3 exact re-runs, but functionally the same check every time); the 16:39:38 user steer "You missed several new added node" triggered a 16:39:44-16:46 fix run. Rule: deterministic `records.py validate` run once per turn end (E5).
9. INITIAL_PROMPT.md re-reads: 158 read commands (97 dedicated calls, 10.42M; 1.58 MB injected; downstream burden 12.8M) of a fixed 7.5 KB file. Already banned in the branch YAML ("Do not re-read INITIAL_PROMPT.md", author_critic.yaml:111, continuation ~213, compaction).
10. Audit reading + post-audit resume: 30 audit-read calls (2.35M; 73 reads of the 55 KB report set, burden 9.1M) plus resume recovery 17:49:25-17:49:46 (4 calls, 0.81M) and 20:13:44-20:41:46 (11 calls, 1.8M); and idle polling: 21 `wait` calls at ~190k context, 2.92M (ROOT 16:08:57, 16:09:45, 16:10:14). Rules: audit-digest (now in YAML), single-report protocol removes polling.

### 3. Duplicate work (measured)
- Lemmas/nodes verified more than once by subagents: 13 IDs: A055 x3 (#118, #119, #124), L023 (#21, #77), A019 (#22, #67), L027 (#24, #37), A026 (#35, #36), A025 (#35, #37), L031 (#37, #43), L042-L044 (#66, #67), A038 (#71, #72), L048 (#75, #77), A044 (#85, #90); A032 audited by both #46 (16:53:34) and #47 (16:55:04); #74 and #94 report "both independent audits passed". Plus the root's own re-read after 60 of these results (item 3 above).
- Files read >5 times by the same thread: 44 (thread,file) pairs. PROVED.md (904 KB): ROOT 79, flow_reduction 31, structural 21, literature 18, audit_cleanup 10. INITIAL_PROMPT.md: flow_reduction 30, literature 25, structural 21, ROOT 15, audit_cleanup 13, batch_supersets 11. A055 (82 KB): audit_cleanup 15, batch_supersets 13, approximation_proofs 9, ROOT 8 = 46 read commands, 0.61 MB injected. A001 (83 KB): 41 reads over 5 threads. ROOT: A034 12, A038 12, A019 11, A013 8, index.md 26. Node reads injected 3.94 MB of the 10.5 MB command output.
- Same arXiv page fetched repeatedly: 2407.10830 x25, 2111.08959 x6, 2512.00176 x5, 2309.16629 x5, 2608.16382 x4, 2504.05687 x4, 2203.00671 x4, 1901.03615 x3, 1910.08025 x3, 2505.14666 x3, 1711.06455 x3.
- Identical validation scripts: 3 exact re-runs (16:03:36 literature hash script; 16:39:35 ROOT index rewrite; 18:56:42 ROOT check); the other 87 checker/hash scripts differ textually but check the same invariants.

### 4. Verification tax
- Direct: 49 verification tasks = 408 subagent calls, 53.05M (19.7%). Root side (approximate): 101 root instruction calls within 2 min before a verification task starts (12.1M) and 60 root re-reads within 3 min after a verification result (7.7M). Upper bound ≈ 73M ≈ 27% of the run; lower bound 20%.
- Yield: all 49 passed. 18 clean, 31 with wording/hypothesis fixes ("replace 'every original arc with tail in A' ...", "assume n>=2", "'five parts' should be 'six'"), 0 substantive errors. The post-mortem (REPORT.md §5) states "no false lemma was found by any analyst". So 20-27% of the run bought wording fixes on lemmas that were correct. The comparable audit_cleanup 18:28-18:56 stretch is twelve consecutive pass verdicts.
- Policy that keeps rigor: (a) verify only lemmas being promoted to PROVED.md, and only those on the PLAN's BEST ROUTE (obstruction lemmas against the author's own proposals stay in the parent's Obstacles section per strategy rule 6 and are not verified separately); (b) exactly once, by one fresh short-lived subagent that receives the lemma statement+proof and the statements (not proofs) of the lemmas it cites, capped at 8 KB, with no workspace browsing and no web; (c) reasoning effort `high`; (d) return PASS/FAIL plus <=150 words; (e) `records.py` stamps "verified by <thread> <time>" under the lemma so nobody re-verifies; (f) root reads the verdict only; a second pass happens only after FAIL or when an auditor disputes the lemma. Expected: ~49 tasks x ~8 calls x ~40k context ≈ 16M (6%) instead of 53-73M.

### 5. Costs inherent to strong research (keep)
- New-mathematics subagent tasks (168 calls, 17.4M, 6.5%) and the mathematical content of root's instruction messages (609 math reasoning items) and thinking-only calls: this is the research. It is small; the run was not expensive because it thought too much.
- Writing proofs into records (part of the 75M): necessary, but with the record diet (abstract + linked lemma IDs instead of restated background) the injected volume drops several-fold; the proving itself must stay.
- Literature for black-box primitives with exact interfaces (roughly a third of the 26M: the 15:06-15:20 status checks and the first read of each of ~10 papers); not the 25 re-fetches or the "is it open?" confirmations.
- One independent verification per promoted lemma (about 6%).
- A coordinator with a large context: the root legitimately holds the PLAN, lemma statements and active nodes (~60-100k); compaction summaries are cheap (0.94M).
- Audits themselves (auditor tokens are not in the transcript; their cost to the author is the 55 KB of reports, which a digest fixes).

### 6. What the current fable-suggestion working tree already implements (uncommitted diff vs main)
DEFAULT_COMPACTION_TOKENS=150k + checkpoint at 120k, continuation delivered on auto-started turns, subagent re-anchor, token meter and budgets, unsaved-result flush, DEFAULT_SPEED "standard", `tools/records.py` (16 KB cap per read), CONTEXT DISCIPLINE and SUBAGENT POLICY (verify once, one-task subagents, max 2 concurrent, 6 fetches/hour) in workflows/author_critic.yaml:108-120, rule 9 against mass migration, audit-digest. Measured against this run: the 150k cap alone touches 719 calls and 22.7M (8.4%); at 120k it is 49.7M (18.4%), at 100k 74.4M (27.6%). The proposals below are the gaps that remain, each sized against this run. Savings overlap (a verification task's interim messages are counted in both E1 and E2); a conservative combined estimate is 50-60% of tokens, plus the 1.5x tier factor on price, with no change to what gets proved.

Not covered here: the xean repository (only its README was skimmed: "Applications provide context, tools, budgets"; its budget/accounting contracts are the analogue of the token meter now on the branch); that comparison belongs to the separate xean analysis.

---

## Proposals

- **[E1] Single-report subagent protocol: no interim messages, no steering of running subagents** (risk low; saving: ~23% of run tokens (61M): 464 interim messages -> ~125 saves ~339 x 120k = 41M; 292 root replies -> ~125 saves ~167 x 125k = 21M; also removes the 21 wait polls (2.9M) and the 133 sibling messages. Assumes the subagent's thinking moves into fewer, longer calls (output tokens unchanged) and that one final report per task suffices.)
  - mechanism: Every subagent->root message and every root->subagent reply is a full-context model call on the receiving side (~120k cached tokens each). Replace the conversation with a contract: one spawn carries one <=2 KB brief; the subagent works silently and returns one final report (<=300 words plus the file it wrote); the root never messages a running subagent and never polls `wait` more than once per task; sibling-to-sibling messages are forbidden. A subagent that is blocked returns BLOCKED with the question instead of chatting.
  - implementation: workflows/author_critic.yaml SUBAGENT POLICY (lines ~116-120): add 'A subagent reports exactly once, at the end; it sends no interim messages and does not message other subagents. The root does not message a running subagent; if it needs a change it waits for the report and spawns a new task.' Runner: in the item/completed handler in workflow_runner.py goal_session (where child answers are collected), count `subAgentActivity` kind=interacted per child thread and emit a status/diagnostic when a child exceeds 1 message; optionally steer the child with 'finish and report'.
  - evidence: eff/tables_activity.txt: 'report to root' 464 calls 56.39M (20.9%), 'delegation msg' 425 calls 54.35M (20.1%) of which ROOT 292/36.67M; median 3 interim messages per task, max 14 (analyze2 output); 21 idle/wait calls 2.92M at ROOT 16:08:57-16:10:14; 133 subagent->subagent messages.
- **[E2] Verify once, at promotion, by a fresh capped verifier that returns a verdict only** (risk low; saving: ~15-20% of run tokens: verification tasks 53.05M (19.7%) plus ~12M root launch/steer and 7.7M root re-reads; replacement ~49 x 8 calls x 40k ≈ 16M. Assumes the verifier's small context (no record browsing) and that obstruction lemmas (16 of 73 lemma titles per post-mortem) are not promoted.)
  - mechanism: Verification cost 20-27% of the run and found zero substantive errors (49/49 passes). Restrict verification to lemmas entering PROVED.md on the PLAN's BEST ROUTE; one fresh short-lived subagent gets the lemma text + statements of cited lemmas (<=8 KB), no workspace browsing, no web, effort high, returns PASS/FAIL + <=150 words; records.py stamps 'verified by' under the lemma; root reads the verdict and does not reopen the node; second passes only after FAIL or an auditor dispute.
  - implementation: workflows/author_critic.yaml:118 already says 'verified once ... when promoted'; extend it with the brief cap, 'no file reads beyond the brief', 'reasoning_effort high', verdict-only return, and 'after PASS do not reopen the node or re-read the lemma'. tools/records.py: add `stamp L034 verified <thread>` that appends a 'Verified:' line under the lemma and make `overview` show unverified promoted lemmas so the root knows what still needs one check. Root rule in CONTEXT DISCIPLINE: 'a lemma with a Verified line is never re-verified'.
  - evidence: 49 verification results: 18 clean passes, 31 minor wording fixes, 0 substantive (results_preview.txt #4,5,9,10,...); REPORT.md §5 'no false lemma was found by any analyst'; 13 IDs verified >=2 times (A055 x3 at #118 21:34:57, #119 21:45:20, #124 21:58:59 = 48 calls 8.30M); 60 ROOT node reads (7.7M) within 3 min after a verification result.
- **[E3] Tier reasoning effort and model per subagent role** (risk medium; saving: ~5-8% of $ (not of tokens): output tokens of verification+writing+formatting = 1.02M of 1.83M; if ultra->high cuts their reasoning tokens ~40%, saves ~0.4M output tokens ≈ $4 of $62 under the stated price model; larger time saving. Assumes output price 8x input; cached input (the bulk) is unaffected.)
  - mechanism: The run used effort 'ultra' for every thread including formatting, migration and verification subagents. Codex's spawn call carries per-child `model` and `reasoningEffort` (the collabAgentToolCall item has both fields), so the root can spawn verification and writing/formatting subagents at 'high' (or a cheaper model) and keep 'ultra' only for itself and new-mathematics solvers.
  - implementation: workflows/author_critic.yaml SUBAGENT POLICY: 'Spawn verification, writing and formatting subagents with reasoning_effort high; use ultra only for a fresh-eyes solver or a new-mathematics task.' No runner change needed; optionally expose `subagent_effort` in the YAML `features`/options and pass it via the spawn tool's reasoningEffort.
  - evidence: job-settings.json reasoningEffort 'ultra' for all roles; transcript collabAgentToolCall items carry 'model': null, 'reasoningEffort': null; output tokens by class (analyze2): writing 683k, verification 262k, formatting 74k, new-math 173k, root 484k.
- **[E4] Lower compaction ceilings: root ~120-130k, subagents ~80k; drop body_after_prefix** (risk medium; saving: Tokens above cap in this run: 150k -> 22.7M (8.4%, branch default); 120k -> 49.7M (18.4%); 100k -> 74.4M (27.6%). Net of extra compactions (~30 more x (30k summary + ~5k recovery) ≈ 1M), a 120k root / 80k subagent setting saves ~15-20% of tokens. Assumes recovery via overview only (compaction prompt already says so).)
  - mechanism: Cached re-reads scale with context length x calls. Contexts ran at p50 118k, p90 193k, max 240k. With the PLAN checkpoint and records.py overview (<=16 KB) making recovery cheap, a lower ceiling converts long tails into more, cheaper windows. One-task subagents do not need >80k.
  - implementation: workflow_runner.py: DEFAULT_COMPACTION_TOKENS 150000 -> 120000 and DEFAULT_CHECKPOINT_TOKENS 120000 -> 95000; remove `context_cache_arguments()` (`model_auto_compact_token_limit_scope="body_after_prefix"`, still passed at the app-server launch in goal_session) so the explicit limit governs. For subagents, add `subagent_compaction_tokens` (80000) passed in the thread config if Codex children inherit it; if they do not (unverified), keep E1's short-lived tasks so their windows stay small by construction.
  - evidence: ctx percentiles from calls_classified.json; compaction items at 167-240k pre-compaction (fatal one at 229k, 22:06:42 usage-limit error); burden model: 202.8M of 269.8M tokens are re-reads of injected material; mean segment 60 calls.
- **[E5] Record diet enforced by the record tool, plus a deterministic validator** (risk low; saving: ~12-15% of tokens: node reads 42.9M direct -> ~12M with capped, sectioned reads (4x fewer bytes), validation 8.9M -> ~1M, PROVED.md reads (79 by ROOT) -> overview lemma titles. Assumes the author obeys the tool rule (it is a prompt rule; the 16 KB cap is the hard part).)
  - mechanism: Node files reached 83 KB (A001, A055) and PROVED.md 904 KB; 356 node-read calls injected 3.94 MB and cost 42.9M directly plus 45M downstream burden; the model also wrote 90 ad-hoc index/hash checker scripts (67 calls, 8.9M). The branch's records.py caps reads at 16 KB per call; add (a) `records.py validate` (reciprocal parent/child links, index rows vs files, link targets, INITIAL_PROMPT hash) run once at turn end, so the model never writes its own checker; (b) a write-side check that refuses a node write >25 KB without a <=40-word abstract; (c) 'lemma L### --full' only for lemmas the current step consumes.
  - implementation: tools/records.py: add `validate` subcommand (reuse the invariants the run's own scripts checked: Parents/Children reciprocity, every A### has one index row, links resolve, INITIAL_PROMPT.md sha256 unchanged) and a `--max-bytes` default already present; workflows/author_critic.yaml file rules: 'Run python3 $TCS_PROVER_TOOLS/records.py validate once at turn end; never write your own checker'; optionally a runner-side check on fileChange items for node size.
  - evidence: ls APPROACHES: A001 83,329 B, A055 82,249 B, A044 69,407 B; PROVED.md 904,102 B; A055 read 46 times (0.61 MB injected); PROVED.md read 79x by ROOT; validate_index 63 + hash_check 27 scripts; 16:39:38 steer 'You missed several new added node' and the 16:39:44-16:46:08 fix.
- **[E6] Shared literature cache: fetch each paper once, siblings read the extract** (risk none; saving: ~3-5% of tokens: 54 redundant fetches of 11 ids (2407.10830 alone: 25 fetches, 2.92M in-call plus burden) and the in-page finds; plus lower burden since extracts are 2 KB instead of ~8k tokens per page. Assumes one extract suffices for interface-level use, which is all strategy rule 7 permits.)
  - mechanism: Page fetches are large injections (literature calls added 1.70M tokens to contexts, the largest injector, burden 48M) and were repeated across threads. Keep the branch's 6-fetch/hour cap and add: before any fetch check `NOTES/papers/<arxiv-id>.md`; after a fetch write a <=2 KB extract (theorem statements with exact interfaces, page/section) there; siblings and later windows read the extract, never the page.
  - implementation: workflows/author_critic.yaml CONTEXT DISCIPLINE web rule (line ~113): replace '/tmp' with a run-local NOTES/papers/ directory allowed in the file layout, and add the check-before-fetch and extract-after-fetch sentences; tools/records.py: `paper <id>` prints the extract. Runner: none.
  - evidence: websearches.txt: 2407.10830 opened 25x (literature_status 14, flow_reduction 10, ROOT 1) 17:52-19:48; 65 fetches of 11 ids opened >=3 times; literature inject 1.70M tokens / burden 48.3M (analyze2 output).
- **[E7] Freeze INITIAL_PROMPT.md during a segment (runner-enforced)** (risk none; saving: ~5% of tokens (106 calls, 14.6M, incl. formatting tasks #26-28, #44-47 = 84 calls 11.17M) per edited run; zero when nobody edits.)
  - mechanism: External edits at 15:32 and 16:02/16:46 made the author and three subagents reformat 32 nodes mid-turn. Rule 9 in the branch YAML tells the author not to migrate; enforce it in the runner: hash INITIAL_PROMPT.md at segment start, and on change pause at the next turn boundary with a status line instead of letting the live turn see the new file.
  - implementation: workflow_runner.py goal_session: in the watch loop that already checks the pause file, compare the sha256 of INITIAL_PROMPT.md to the value taken at start; on mismatch set the pause flag and emit 'INITIAL_PROMPT.md changed; applying at segment boundary'. Optionally chmod a-w the file while the author runs.
  - evidence: narrative.txt 15:32:22 'Adapting to external prompt changes', 16:02:09 'Noticing prompt layout changes', 16:51:20 'Processing prompt updates'; migration windows total 106 calls 14.60M; subagent results #26-28 (16:06-16:07) and #44-47 (16:49-16:56).
- **[E8] Standard service tier instead of fast (already flipped on the branch; keep it)** (risk none; saving: 33% of $ at equal tokens (≈$93 -> ≈$62 under the stated price model), if the fast multiplier is 1.5x as the README states; 0% of tokens.)
  - mechanism: The run passed `service_tier="fast"` + `fast_mode`, which the README describes as OpenAI's 1.5x Fast mode. Speed is 'less important' per the user; standard tier removes the multiplier with identical model output.
  - implementation: workflow_runner.py: DEFAULT_SPEED already changed 'fast' -> 'standard' in the working tree (SPEEDS line); keep it and expose per-role speed so only the root could use fast if wall time ever matters.
  - evidence: job-settings.json speedMode 'fast'; workflow_runner.py speed_arguments() returns ['-c','service_tier="fast"','--enable','fast_mode']; README/ docstring 'Standard or OpenAI's 1.5x Fast mode'.
- **[E9] Audit digest and resume without re-reading (mostly on the branch; add the per-verdict tally)** (risk none; saving: ~3-4% of tokens: audit reads 2.35M in-call + 9.1M burden, resume recovery 2.82M; digest ≈ 4 KB read twice.)
  - mechanism: After each audit batch the root re-read INITIAL_PROMPT.md, index, PROVED.md tail and all three reports (17-25 KB each), then subagents read them again (73 reads of the 55 KB set). The branch adds audit-digest.md; make the runner write it (one page: per-node STOP/CONTINUE tally, union of directions) so no model call spends tokens summarising reports, and tell subagents never to open AUDITS/.
  - implementation: workflows/research_audit.yaml already gained the phased prompt; add a post-audit step in workflow_runner.py (where audit reports are saved to AUDITS/) that parses 'STOP/CONTINUE/REDIRECT <A###>' lines into audit-digest.md; author_critic.yaml resume prompt already reads the digest first; add to SUBAGENT POLICY 'subagents do not read AUDITS/'.
  - evidence: read:audits 30 calls 2.35M, 1.1 MB injected; resume recovery 17:49:25-17:49:46 (0.81M), 20:13:44-20:41:46; control_events 17:00:06-17:49:23 and 19:49:23-20:13:25 pauses (62 min).
